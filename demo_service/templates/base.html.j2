<!DOCTYPE html>
<html lang="en" class="h-100">
    <head>
        <!-- set the encoding of your site -->
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <link rel="icon" href="{{ url_for('static', filename='images/logo1.1_notext.svg') }}" />

        <title>Hackspace Design Demo</title>
        <meta property="og:title" content="Hackspace Design Demo" />
        <meta property="og:type" content="website" />
        <meta property="og:url" content="/" />
        <!-- <meta property="og:image" content="images/logo300x300.png"> -->

        <!-- Custom styles for this template -->
        {% assets filters='cssmin', output="gen/all.css",
            "css/base.css", "css/header.css", "css/component.css", "css/form.css" %}
        <link href="{{ ASSET_URL }}" rel="stylesheet">
        {% endassets %}
    </head>
    <body>
        <header>
            <nav class="navbar">
                <a href="#" class="nav-title">
                    <img src="{{ url_for('static', filename='images/pride halo logo.png') }}" alt="">
                    <span>Bristol<br/>Hackspace</span>
                </a>
                <ul class="nav-menu">
                    <li><a href="#" class="nav-link">Things</a></li>
                    <li><a href="#" class="nav-link">Other things</a></li>
                </ul>
            </nav>
        </header>

        <main class="container">
            {% block contents %}
            {% endblock %}
        </main>

        <footer class="container">
            <p>© Copyright 2026 Bristol Hackspace.</p>
        </footer>
    </body>
</html>